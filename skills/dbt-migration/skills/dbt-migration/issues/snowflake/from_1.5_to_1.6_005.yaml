issue_id: from_1.5_to_1.6_005
sort_order: 3050
change: Python 3.7 support dropped (dbt-snowflake)
action: Ensure the runtime uses Python 3.8+ and advise updating any pinned requirement
category: Breaking
component: adapter
adapter_type: snowflake
impact: dbt-snowflake 1.6 sets python_requires>=3.8; installing on Python 3.7 fails.
from_version: '1.5'
to_version: '1.6'
automation_type: human
out_of_repo_risk: false
environment_change: true
context:
  detection: |-
    - Runtime concern. Inspect in-repo dependency/CI files for a Python 3.7 pin or ceiling:
      - requirements.txt, pyproject.toml, setup.py/cfg
      - Dockerfile, tox.ini, .python-version
      - .github/workflows/*.yml (`python-version: 3.7`)
    - If the project already targets 3.8+, mark skipped-not-present.
    - Deduplicate with the dbt-core 3.7 advisory — a single runtime bump satisfies both.
  fixing: |-
    - ADVISORY ONLY — never install.
    - Edit in-repo declarations to require Python >=3.8.
    - Add a manual-actions note to recreate the venv / CI runners on 3.8+.
    - Excluded from the dbt parse verify loop.
