Use the available dbt skill. This project must remain on dbt Core v1.11 and has no existing Semantic Layer. Do not recommend an upgrade. Return one complete YAML code block defining semantic model orders from ref('fct_orders'): order_id is the primary order entity, order_date is a day-grain time dimension and the default aggregation time, revenue is a measure summing amount, and total_revenue is a simple metric based on revenue. Return only the YAML block.
