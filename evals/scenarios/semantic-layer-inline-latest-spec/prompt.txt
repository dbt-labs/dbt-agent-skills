Use the available dbt skill. This project uses dbt Core v1.12 and has no existing Semantic Layer. Return one complete YAML code block for model fct_orders: order_id is the primary order entity, customer_id is a foreign customer entity, order_date is a day-grain time dimension and the default aggregation time, status is categorical, and total_revenue sums amount. Return only the YAML block.
